apiVersion: v1
kind: Service
metadata:
  name: sonar
  labels:
    name: sonar
spec:
  type: LoadBalancer
  loadBalancerIP: 10.0.0.200
  ports:
    - port: 9000
      targetPort: 9000
      protocol: TCP
  selector:
    name: sonar
